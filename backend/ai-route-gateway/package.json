{
  "name": "todayplay-ai-route-gateway",
  "version": "0.9.60",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node tests/smoke.test.mjs",
    "dev": "wrangler dev worker.js --port 8787",
    "dev:android": "wrangler dev worker.js --ip 0.0.0.0 --port 8787",
    "deploy": "wrangler deploy worker.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
