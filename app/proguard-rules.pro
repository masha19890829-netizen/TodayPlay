# Release shrinking is enabled for Google Play readiness.
# Android Gradle Plugin and library consumer rules keep Compose, Activity, and Billing entrypoints.
# Add explicit rules here only when release builds or runtime smoke tests prove a class is stripped incorrectly.
